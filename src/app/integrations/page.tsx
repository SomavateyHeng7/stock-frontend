import { IntegrationsSyncHub } from "@/components/integrations-sync-hub";

export default function IntegrationsPage() {
  return <IntegrationsSyncHub title="Integrations + Sync" subtitle="Test live connectors and resolve multi-channel inventory mismatches in one place." />;
}
