import { IntegrationsSyncHub } from "@/components/integrations-sync-hub";

export default function SyncPage() {
  return <IntegrationsSyncHub title="Integrations + Sync" subtitle="Connect external channels and keep channel inventory reconciled from one workspace." />;
}
